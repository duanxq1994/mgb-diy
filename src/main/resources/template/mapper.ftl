package ${targetPackage};

import org.springframework.stereotype.Repository;

/**
 * @author mbg
 * Created by mbg on ${.now?string('yyyy-MM-hh')}.
 */
@Repository
public interface ${tableClass.shortClassName}${suffix} {

}